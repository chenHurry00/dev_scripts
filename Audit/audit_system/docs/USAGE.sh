#!/bin/bash
# 审计系统 - 完整使用指南

cat << 'EOF'
╔════════════════════════════════════════════════════════════╗
║     Linux 命令审计系统 - 使用指南                        ║
╚════════════════════════════════════════════════════════════╝

✅ 已完成的修复
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【问题 1】只记录 Web 访问，看不到 Linux 命令
  ✓ 已禁用 Web 访问日志记录
  ✓ 只记录 Linux 终端命令

【问题 2】看不到具体命令内容
  ✓ 现在显示完整命令：ls -la /home/yuchen
  ✓ 自动分类：FILE、SYSTEM、NETWORK 等
  ✓ 自动风险评估：L1-INFO 到 L5-CRITICAL

【问题 3】后台刷新看不到命令
  ✓ 命令实时写入数据库
  ✓ Web 界面刷新即可看到

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 快速开始（新终端）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【步骤 1】启用命令审计（在新终端执行）

  source ~/.bashrc

  说明：加载 bash 钩子，之后执行的所有命令都会被记录


【步骤 2】执行一些命令测试

  ls -la
  cat /etc/hosts
  cd /tmp
  pwd
  sudo systemctl status nginx


【步骤 3】查看 Web 界面

  访问：http://localhost:5000
  登录：admin / BY116358

  现在可以看到：
  ✓ 用户名：yuchen（而不是 anonymous）
  ✓ 操作类型：FILE_READ、SYSTEM_ADMIN 等
  ✓ 目标资源：完整的命令内容
  ✓ 风险级别：L1-INFO、L4-HIGH 等

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 命令分类示例
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

L5 - CRITICAL  | rm -rf /, dd if=/dev/zero, mkfs
L4 - HIGH      | sudo systemctl restart, reboot, shutdown
L3 - MEDIUM    | chmod 777, curl, mysql, apt install
L2 - LOW       | echo, python3, cd
L1 - INFO      | ls, cat, grep, find

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 Web 界面查看示例
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

时间                用户    分类    操作类型        目标资源（命令）           风险
--------------------------------------------------------------------------------
2026-05-28 15:00:00 yuchen  FILE    FILE_READ       ls -la /home/yuchen        L1-INFO
2026-05-28 15:00:05 yuchen  FILE    FILE_READ       cat /etc/hosts             L1-INFO
2026-05-28 15:00:10 yuchen  SYSTEM  SYSTEM_ADMIN    sudo systemctl restart     L4-HIGH
2026-05-28 15:00:15 yuchen  SYSTEM  COMMAND_EXEC    cd /tmp                    L2-LOW

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 筛选和查询
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【在 Web 界面】

1. 查看所有 yuchen 用户的命令
   - 用户名：yuchen
   - 点击"查询"

2. 查看高危操作
   - 用户名：yuchen
   - 最小风险级别：L4-HIGH
   - 点击"查询"

3. 查看文件操作
   - 用户名：yuchen
   - 操作分类：FILE
   - 点击"查询"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚙️ 工作原理
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 用户执行命令：ls -la
   ↓
2. Bash PROMPT_COMMAND 钩子触发
   ↓
3. 调用 audit_command.py
   ↓
4. 实时写入数据库
   ↓
5. Web 界面刷新即可看到

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 相关文件
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

audit_system/
├── audit_command.py              # 命令记录脚本（实时写入数据库）
├── audit_bash_hook.sh            # Bash 钩子
├── install_command_audit.sh      # 安装脚本
├── test_command_audit.sh         # 测试脚本
├── COMMAND_AUDIT.md              # 完整文档
└── data/audit.db                 # 数据库（已清理旧日志）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 重要说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ✅ 只记录 Linux 终端命令
   - 不记录 Web 界面访问（登录、页面浏览等）
   - 只记录真实的 Linux 用户操作

2. ✅ 实时同步
   - 命令执行后立即写入数据库
   - Web 界面刷新即可看到
   - 无需手动导入

3. ✅ 完整命令内容
   - 不只是分类，还有具体命令
   - 包含工作目录、主机名等信息

4. ⚠️ 需要在新终端生效
   - 当前终端需要执行：source ~/.bashrc
   - 或者打开新终端自动生效

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 功能完成！现在可以正常使用了！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EOF
